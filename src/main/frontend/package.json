{
  "name": "helioauth-demo-frontend",
  "private": true,
  "scripts": {
    "build": "esbuild app.js --sourcemap --bundle --outfile=../resources/static/app.js"
  },
  "dependencies": {
    "@github/webauthn-json": "^2.1.1",
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "esbuild": "0.20.2"
  }
}
